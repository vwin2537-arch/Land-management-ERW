# 🛠️ คู่มือการตั้งค่าโปรเจกต์ (Setup Guide)
*ใช้สำหรับติดตั้งเมื่อย้ายเครื่องหรือลง XAMPP ใหม่*

---

## 🔗 1. การเชื่อมต่อโฟลเดอร์ (Symbolic Link)
เพื่อให้ XAMPP รันโปรเจกต์จาก OneDrive ได้โดยไม่ต้องย้ายไฟล์

1. เปิด **CMD (Command Prompt)** ด้วยสิทธิ์ **Administrator**
2. รันคำสั่งนี้:
```cmd
mklink /D "C:\xampp\htdocs\PHP_SQL" "C:\Users\This PC\OneDrive\000_Ai Project\PHP_SQL"
```

---

## 🗄️ 2. การตั้งค่าฐานข้อมูล (Database)
1. เปิด **XAMPP Control Panel** และ Start **MySQL**
2. รันคำสั่งสร้างฐานข้อมูลและนำเข้าข้อมูลผ่าน **CMD**:
   *(รวดเร็วกว่า phpMyAdmin และไม่ค้าง)*

**สร้างฐานข้อมูล:**
```cmd
C:\xampp\mysql\bin\mysql -u root -e "CREATE DATABASE land_management CHARACTER SET utf8mb4 COLLATE utf8mb4_thai_520_w2;"
```

**นำเข้าข้อมูล (Import Schema):**
```cmd
C:\xampp\mysql\bin\mysql -u root land_management < "C:\Users\This PC\OneDrive\000_Ai Project\PHP_SQL\sql\schema.sql"
```

---

## 🌐 3. ลิงก์สำคัญ (Local Links)
*   **หน้าเว็บโปรเจกต์:** [http://localhost/PHP_SQL/](http://localhost/PHP_SQL/)
*   **จัดการฐานข้อมูล:** [http://localhost/phpmyadmin/](http://localhost/phpmyadmin/)

---

## ⚠️ ข้อควรระวัง
- **Database Config**: หากมีการตั้งรหัสผ่าน MySQL ให้ไปแก้ไขที่ไฟล์ `config/database.php`
- **Path**: หากชื่อ User ใน Windows เปลี่ยนไป (ไม่ใช่ "This PC") ให้แก้พาธในคำสั่ง `mklink` ให้ตรงกับเครื่องปัจจุบัน
