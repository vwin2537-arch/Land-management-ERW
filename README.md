# วิธีการติดตั้งและรันโปรเจกต์ (Getting Started)

ระบบจัดการที่ดินทำกินในเขตอุทยานแห่งชาติ พัฒนาด้วย PHP และ MySQL

## 📋 ความต้องการของระบบ (Requirements)
- **XAMPP** หรือ **WAMP** (รองรับ PHP 8.0 ขึ้นไป)
- **MySQL / MariaDB**

## 🚀 ขั้นตอนการติดตั้ง
1.  **ย้ายไฟล์โปรเจกต์**:
    - คัดลอกโฟลเดอร์ `PHP_SQL` ไปไว้ในโฟลเดอร์ `htdocs` (สำหรับ XAMPP) หรือ `www` (สำหรับ WAMP)
2.  **ตั้งค่าฐานข้อมูล**:
    - เปิดโปรแกรม XAMPP Control Panel และกดปุ่ม **Start** ที่ Apache และ MySQL
    - เข้าไปที่ [http://localhost/phpmyadmin](http://localhost/phpmyadmin)
    - สร้างฐานข้อมูลใหม่ชื่อ: `land_management`
    - กดที่ฐานข้อมูลที่สร้างขึ้น แล้วเลือกเมนู **Import**
    - เลือกไฟล์จาก `sql/schema.sql` ในโฟลเดอร์โปรเจกต์ แล้วกด **Go / Import**
3.  **ตั้งค่าการเชื่อมต่อ**:
    - หากคุณตั้งค่า Username/Password ของ MySQL ต่างจากมาตรฐาน (root/ไม่มี password)
    - ให้แก้ไขที่ไฟล์ `config/database.php`

## 🔑 การเข้าใช้งาน
- เปิดเบราว์เซอร์แล้วไปที่: `http://localhost/PHP_SQL`
- **Username**: `admin`
- **Password**: `admin123`

---

## 📂 โครงสร้างที่สำคัญ
- `index.php`: จุดเริ่มต้นของโปรแกรม
- `config/`: ตั้งค่าฐานข้อมูลและค่าคงที่ต่างๆ
- `controllers/`: ส่วนควบคุมการทำงาน (Logic)
- `views/`: ส่วนการแสดงผล (UI)
- `uploads/`: โฟลเดอร์เก็บไฟล์ภาพและเอกสารที่อัปโหลด
